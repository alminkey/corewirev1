# Workspace Layout

The active CoreWire application now lives in:

```text
F:\2026\CoreWire\CooreWireV1
```

What stays at this outer repo root:

- `docs/superpowers/*videeditor*`: notes for the unrelated VideoEditor effort
- `code.html`, `screen.png`, `vjecno-povrce.html`: external/reference assets
- `.worktrees`, `.superpowers`, `.tmp`: development infrastructure

If you want to run or edit CoreWire itself, start from `CooreWireV1`.
