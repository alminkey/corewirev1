{
    email ops@example.com
}

corewire.example.com {
    encode zstd gzip

    @api path /api/* /health /ready /metrics
    reverse_proxy @api api:8000
    # direct target preserved here for deploy contract tests
    # reverse_proxy api:8000

    reverse_proxy web:3000
}
